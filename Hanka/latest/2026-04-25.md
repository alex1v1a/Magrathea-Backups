# Memory Log - 2026-04-25

## Email flow corrected to IMAP/iCloud-only
- Removed local-only archive fallback from `email-auto`; spam/archive/apply paths now use IMAP/iCloud sync only.
- Kept Mail refresh as an optional local-view helper only, not part of the archive success path.
- Fixed `scripts/archive_imap_icloud.py` to exit non-zero on real errors instead of silently succeeding.
- Fixed `scripts/edit_mail_rules.py` so direct `add` creates exact-sender rules by default; added `delete` support for bad rules.
- Removed an accidentally-created broad rule `Auto-archive: *@gmail.com` and replaced it with exact-sender handling for Thomas Pantano.
- Added spam handling for: `thomaspantano@gmail.com`, `no-reply@eclinicalmail.com`, `news@info.gohealthuc.com`, and `noreply@email.wyndhamvo.com`.
- Confirmed IMAP archive succeeded for `news@info.gohealthuc.com`; the others had no currently-found messages during the IMAP pass.
- Broadened spam policy to domain-family auto-archive for `newyorktimes.com`, `kickstartgenius.com`, `kickstarternew.com`, `kickstarnews.com`, and `backerhive.com` after user reported leaks.
- Reconciled Mail rules from typed policy state so those domains now have managed auto-archive coverage.
- Applied IMAP archive pass to current inbox for all five domains; no currently-found messages were returned by IMAP during this pass even though local inbox inspection had shown `nytimes@e.newyorktimes.com` earlier.
- Noted classifier/history inconsistency: some of these senders had prior `inbox_keep` message decisions despite being intended spam.
- User reported additional inbox leaks: "Tony Patino" and Kickstarter project mail.
- Located active Kickstarter-project sender as `email@kickstarproject.com`; added deny policy for `@kickstarproject.com` and reaffirmed `@kickstarter.com`, reconciled Mail rules, and archived 1 live inbox message via iCloud IMAP.
- Searched local mail index for `Tony Patino`; no exact match found. Closest current recruiter-style Tony sender found was `tony@careerdevelopers.com`.
- Email preference from alex1v1a: keep `no_reply@email.apple.com` and mail from `@email.apple.com` out of spam / auto-archive.
- Fixed conflicting email policy state: removed Apple sender/domain from deny rules, added explicit keep rules, and deleted stale `learned_spam` decision records that were overriding the keep intent.
- Heartbeat check at 2026-04-25 07:24 CT: local storage OK (~12 GiB free on /System/Volumes/Data, above 5 GiB threshold).
- `openclaw status` succeeded: Discord token/account OK; gateway service running locally, though status reports loopback gateway unreachable due missing `operator.read` scope rather than a local crash.
- Ran `scripts/team-health-monitor.sh`: Marvin SSH/gateway OK, Deep Thought SSH/gateway OK, Bistromath SSH FAILED.
- Follow-up probes from Trillian host to Bistromath (10.0.1.9) timed out on TCP 22, 3389, and 18789; this looks like machine/network unreachable rather than just SSH auth failure.
- No remote recovery path available from current host while all three ports time out. Likely causes: machine offline, network path down, or Windows not yet accepting connections after reboot.
- Heartbeat check at 2026-04-25 13:10 CT: local storage still OK (~12 GiB free on /System/Volumes/Data, above 5 GiB threshold).
- `openclaw status` still shows local gateway service running; loopback reachability warning remains a missing `operator.read` scope issue rather than a service crash. Discord account/token still appear OK.
- Re-ran `scripts/team-health-monitor.sh`: Marvin SSH/gateway OK, Deep Thought SSH/gateway OK, Bistromath SSH FAILED.
- Fresh reachability probe to Bistromath (10.0.1.9) now returns `Host is down` on ports 22, 3389, 18789, 7878, 8989, 8686, 9696, and 8080, which strongly suggests the box is offline or unreachable on the LAN, not merely a single-service failure.
- The inbound `dawn-ott` arr-check failure is therefore consistent with Bistromath being down rather than Radarr/Sonarr alone misbehaving.
- Added `scripts/mark_imap_folders_read.py` to mark unread messages as read via iCloud IMAP in Archive/Junk folders.
- Installed daily cron at 5:00 PM CT: `0 17 * * * cd /Users/hanka/.openclaw/workspace && /usr/bin/python3 scripts/mark_imap_folders_read.py >> /Users/hanka/.openclaw/logs/mark-imap-folders-read.log 2>&1`
- Validated with dry run, then live run: marked 43 unread Archive messages and 5 unread Junk messages as read (48 total) via iCloud IMAP sync.
- Heartbeat fix at 2026-04-25 21:08 CT: Marvin service monitor reported the Marvin gateway DOWN and failed recovery.
- Diagnosis: the gateway service on Marvin was actually running, but `gateway.bind` had drifted to `loopback`, so it was only listening on `127.0.0.1` and appeared dead to off-box checks.
- Verified remotely on Marvin: `openclaw gateway status` showed `Listening: 127.0.0.1:18789`; `openclaw qr --json` failed with “Gateway is only bound to loopback”.
- Recovery: set `gateway.bind=lan`, restarted the Marvin gateway, then verified `Listening: 0.0.0.0:18789` and confirmed `http://10.0.1.90:18789/` returned HTTP 200 from Trillian.
- Re-ran `/Users/hanka/.openclaw/scripts/marvin-service-monitor.sh`; Home Assistant, Mission Control Dashboard, Mission Control API, and Marvin Gateway all reported UP.
- Follow-up fleet action at 2026-04-25 21:13 CT: ran `scripts/team-health-monitor.sh` after the Marvin fix; Marvin, Deep Thought, and Bistromath all reported SSH OK and Gateway reachable.
- Earlier Bistromath gateway timeout was transient during restart/warm-up. Remote probe initially timed out, but local verification on Bistromath returned HTTP 200 from `http://127.0.0.1:18789/`, and subsequent LAN verification from Trillian returned HTTP 200 from `http://10.0.1.9:18789/`.
- Ran `openclaw security audit --deep` on Trillian host: notable findings included short gateway token, permissive exec/full trust, multi-user trust-model warning, and discord-voice plugin exposure/code-safety concerns.
- Applied safe built-in fixes with `openclaw security audit --fix`; this tightened local file/session permissions and reduced the immediate audit from 2 critical/5 warn to 0 critical/4 warn.
- Remaining deferred items after validation are policy-level/riskier changes rather than break-fix work: rotate to a long gateway token, reduce exec trust from `full`, revisit multi-user/shared-gateway exposure, and review/remove or constrain the local `discord-voice` extension.
